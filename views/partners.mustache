
<div class="intro">
	{{{ intro }}}
</div>
<img class="space_after" src="{{ image.url }}" alt="{{ image.desc }}">
<h1 class="headline_1">{{ title }}</h1>
<ul class="partners">
	{{#partner}}
		<li>
			{{>partner}}
	{{/partner}}
</ul>